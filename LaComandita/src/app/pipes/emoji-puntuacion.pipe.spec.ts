import { EmojiPuntuacionPipe } from './emoji-puntuacion.pipe';

describe('EmojiPuntuacionPipe', () => {
  it('create an instance', () => {
    const pipe = new EmojiPuntuacionPipe();
    expect(pipe).toBeTruthy();
  });
});
