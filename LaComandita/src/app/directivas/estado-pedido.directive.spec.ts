import { EstadoPedidoDirective } from './estado-pedido.directive';

describe('EstadoPedidoDirective', () => {
  it('should create an instance', () => {
    const directive = new EstadoPedidoDirective();
    expect(directive).toBeTruthy();
  });
});
