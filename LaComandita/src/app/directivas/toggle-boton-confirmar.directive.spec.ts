import { ToggleBotonConfirmarDirective } from './toggle-boton-confirmar.directive';

describe('ToggleBotonConfirmarDirective', () => {
  it('should create an instance', () => {
    const directive = new ToggleBotonConfirmarDirective();
    expect(directive).toBeTruthy();
  });
});
