import { ColorPuntuacionPipe } from './color-puntuacion.pipe';

describe('ColorPuntuacionPipe', () => {
  it('create an instance', () => {
    const pipe = new ColorPuntuacionPipe();
    expect(pipe).toBeTruthy();
  });
});
